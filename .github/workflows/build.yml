name: Node.js CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
      - run: npm install
      - run: npm ci
      - run: npm run build --if-present
      - run: npm run test:once
